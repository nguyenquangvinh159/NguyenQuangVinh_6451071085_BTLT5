import 'package:flutter/material.dart';

class MyLearningTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("My Learning Courses"),
    );
  }
}